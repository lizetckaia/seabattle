#include "main.cpp"
#include <doctest.h>

TEST_CASE("doctest works")
{
    REQUIRE(true);
}

TEST_CASE("testing map_show")
{

}

TEST_CASE("testing shot")

{
    int shot;
    CHECK (result == 3);
    CHECK (result == 2);
    CHECK (result == 1);
    CHECK (result == -1);
}

